# Expense App built on React + Vite

It is a basic Expenses App which is built using react.js

# Installation

1. Clone the repo
2. Install the dependencies using `npm install`
3. Start the server using `npm run dev`

